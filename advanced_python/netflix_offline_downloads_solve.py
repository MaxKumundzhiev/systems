"""
Netflix “Offline Downloads”: Скачивание и упаковка сезона в устройство

Контекст
Пользователь скачивает сезон сериала для оффлайна. Данные приходят как stream, общий размер может быть десятки GiB.
На устройстве/в сторидже ограничение: один файл не больше 200 MiB.

Проблема
Нельзя держать весь сезон в памяти
read(size) может возвращать меньше
Шифрование + сжатие CPU-heavy

Запись в “device storage” медленная, надо перекрывать процессинг и запись

Требования
Реализовать DownloadPackManager.download(in_stream) и restore(out_stream)
Стриминговый pipeline (read → process → write) с bounded очередями
Корректная схема именования чанков и сборка в правильном порядке
Без блокировки event loop на синхронном read

Тесты
A) read() возвращает “рваными” кусками → чанки строго 200 MiB
B) list_files() возвращает случайный порядок → restore собирает правильно
C) Ограничение RAM: очередь maxsize=2, при медленной записи producer должен тормозить (backpressure)

Вопросы
ThreadPool vs ProcessPool и почему
как избежать “бутылочного горлышка” записи
как сделать resume после падения
"""
